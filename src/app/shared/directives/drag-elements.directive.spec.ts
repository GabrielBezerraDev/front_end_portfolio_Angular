import { DragElementsDirective } from './drag-elements.directive';

describe('DragElementsDirective', () => {
  it('should create an instance', () => {
    const directive = new DragElementsDirective();
    expect(directive).toBeTruthy();
  });
});
