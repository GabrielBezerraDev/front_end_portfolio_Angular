import { HoverElementsDirective } from './hover-elements.directive';

describe('HoverElementsDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverElementsDirective();
    expect(directive).toBeTruthy();
  });
});
